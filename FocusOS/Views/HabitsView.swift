import SwiftUI

struct HabitsView: View {
    var body: some View {
        VStack {
            Text("Habits")
                .font(.largeTitle)
                .padding()
            Text("Habit tracking functionality coming soon.")
                .foregroundColor(.gray)
        }
    }
}
