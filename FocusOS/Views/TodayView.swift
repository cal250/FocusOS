import SwiftUI

struct TodayView: View {
    var body: some View {
        VStack {
            Text("Today")
                .font(.largeTitle)
                .padding()
            Text("Tasks and Schedule implementation coming soon.")
                .foregroundColor(.gray)
        }
    }
}
